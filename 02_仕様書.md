# 仕様書:単振り子サロゲートモデル 原理検証システム

- 文書番号: SPEC-SURR-001
- 版数: Rev.1.0
- 対応要件定義書: REQ-SURR-001

## 1. システム概要

本システムは、単振り子を対象として (1) 物理モデル、(2) 学習データ生成、(3) NSSサロゲートモデル、
(4) オープンループ検証、(5) 閉ループ検証、の5要素から構成される。全体構成は以下の通り。

```
[物理モデル(MATLAB)] --シミュレーション--> [学習データ(.mat/.csv)]
        |                                        |
        |                                        v
        |                              [NSS学習(Python/PyTorch)]
        |                                        |
        |                                        v
        |                              [サロゲートモデル(.onnx等)]
        |                                        |
        v                                        v
[コントローラ + 物理モデル 閉ループ] ---比較--- [コントローラ + サロゲート 閉ループ]
                                        |
                                        v
                              [診断指標・レポート]
```

## 2. 物理モデル仕様(単振り子)

### 2.1 運動方程式

減衰・重力項を含む非線形1自由度系とする。

```
J * θ_ddot + b * θ_dot + m * g * l * sin(θ) = τ
```

- θ [rad]: 関節角度(出力)
- θ_dot [rad/s]: 角速度
- τ [N·m]: 入力トルク(入力)
- J [kg·m^2]: 慣性(モータ+リンク換算)
- b [N·m·s/rad]: 粘性摩擦係数
- m [kg]: リンク質量、l [m]: 重心までの長さ、g = 9.80665 [m/s^2]

### 2.2 状態空間表現

```
x = [θ, θ_dot]^T
x_dot = f(x, τ) = [ θ_dot ,
                     (τ - b*θ_dot - m*g*l*sin(θ)) / J ]^T
y = θ   (角度センサ相当。物理モデルの出力はθ_dotも含めるオプションを許容。
         サロゲートモデルの出力形式(角度/角速度/角加速度)の段階的検討方針は§3.1参照)
```

### 2.3 パラメータ標準値(仮定・要調整)

| パラメータ | 値(目安) | 備考 |
|---|---|---|
| J | 0.02 kg·m^2 | 6軸ロボット1関節の縮約値イメージ |
| b | 0.05 N·m·s/rad | 粘性摩擦 |
| m*l (等価) | 0.15 kg·m | mgl項として使用 |
| τ_max | ±3.0 N·m | アクチュエータ飽和 |
| θ範囲 | -π/2 〜 +π/2 rad | PTP動作の可動範囲 |

パラメータは6軸ロボットの1関節の代表的な慣性・トルクオーダーに合わせて調整すること
(要件N-03: 一般化容易性)。

### 2.4 実装

- MATLAB `ode45`(または固定ステップ`ode4`)による数値積分でリファレンス応答を生成
- Simulinkモデルとしても構築し、閉ループ検証時にコントローラと結合可能な形とする
- 物理モデル・コントローラの制御周期は **8kHz(Ts_ctrl = 125μs)を基本とする**。
  サロゲートモデルはこれと同一の8kHzで動作させてもよいが、**200Hz(Ts_surr = 5ms、
  デシメーション比 40:1)へダウンサンプルすることも可**とする(詳細は§6.4)。

## 3. NSSサロゲートモデル仕様

### 3.1 モデル構造

Neural State Space(離散時間)として以下を定義する。

```
x_{k+1} = f_θ(x_k, u_k)      … 状態遷移ネットワーク
y_k     = g_φ(x_k)            … 出力ネットワーク
```

- 入力 u_k: トルク τ_k(スカラー)
- 内部状態 x_k: 次元 n_x(初期検証では物理状態と同じ n_x=2 から開始し、
  必要に応じ潜在次元を増やして比較する)
- 添字kの時間刻みはNSSの動作周期 Ts_surr に対応する。Ts_surr は制御周期と同一の8kHz、
  または200Hzへダウンサンプルしたものかを明示的に選択する(§6.4)。学習データのTsは
  Ts_surr と一致させること。

#### 出力形式の段階的検討方針

出力 y_k の物理量は、角度・角速度・角加速度およびそれらの組み合わせの中から
段階的に検討する。各段階は、閉ループ検証(§6.2)で発散・精度不足が確認された場合に
次段階へ進む(§4.1のデータ生成方針と同様の進め方)。

| 段階 | 出力 y_k | コントローラへのフィードバック信号の作り方 |
|---|---|---|
| Step1(基本) | θ_k(角度) | y_kをそのままθフィードバックに使用。θ_dotが必要な場合は
  内部状態x_k(n_x=2の場合は状態そのものがθ_dot相当)または差分近似から得る |
| Step2(Step1で不十分な場合) | θ_dot_k(角速度)、または[θ_dot_k, θ_ddot_k] | y_kを数値積分
  (台形則等)してθを再構成し、θ・θ_dotの両方をフィードバックに利用する |
| Step2'(併用検討) | θ_ddot_k(角加速度)単独、または θ_k と θ_dot_k / θ_ddot_k の組み合わせ | 出力済みの
  低次微分量(角度・角速度)はそのまま使用し、欠けている高次微分量のみ数値積分で補う |

- 角速度・角加速度を出力として積分により角度を再構成する場合、積分に伴うドリフト
  (バイアス蓄積)の評価を必須とする(§6.1の長時間ロールアウト検証で確認)。
- どの段階の出力形式を採用したかは、モデルごとに記録し、§6.1〜6.3の指標比較時に
  出力形式の違いによる差分として明示すること。

### 3.2 ネットワーク仕様(初期案)

| 項目 | 仕様 |
|---|---|
| f_θ | MLP、隠れ層2〜3層、各層32〜64ユニット、活性化関数 tanh または SiLU |
| g_φ | 線形層 または恒等写像(状態に角度を直接含める設計を推奨) |
| 出力層 | 活性化関数なし(線形) |
| 正規化 | 入出力ともに学習データの平均・分散で標準化 |

tanh等の有界な活性化関数は状態遷移の急激な発散を抑制しやすいため、初期案として推奨する
(ReLU系は外挿領域で線形に増大し発散しやすい点に留意)。

### 3.3 安定性に関する設計要件

| ID | 要件 |
|---|---|
| M-01 | f_θ の出力(状態増分)にはスケール制約(例: tanhによる飽和、またはスペクトル正規化)を検討する |
| M-02 | 平衡点(θ=0, θ_dot=0, τ=0相当)近傍でのヤコビアン固有値を学習後に必ず算出し、報告する |
| M-03 | 可能であれば、物理事前知識(ポート・ハミルトニアン構造、エネルギー保存則)を組み込んだ
  構造化NSS(Physics-Informed / Structured NSS)を比較対象として用意する |
| M-04 | **入出力方向整合性(局所ゲイン符号)の検証(初期段階は診断のみ、制約なし)**。
  f_θ・g_φの合成による入出力感度 ∂ŷ_{k+1}/∂τ_k(数値微分でよい)を、学習・検証データの
  動作軌道上の各時刻で算出し、物理モデルの符号(τ>0でθが増加する方向)と一致するかを
  確認する。この制約に違反する領域(符号反転領域)が存在すると、コントローラの誤差
  補正トルクに対して逆方向の応答が返り、補正がさらに大きな補正指令を誘発する
  **正のフィードバックループ**となって発散し得る(発散原因候補②「力学的不安定性・
  受動性の破れ」の具体的機序)。**初期実装ではこの方向制約をモデルに課さず(M-01の
  スケール制約のみを適用した標準的なMLP構成のまま)、符号反転領域の検出・記録のみを
  行う。**§6.3実験Fでこの仮説が発散原因として確認された場合に限り、方向制約付き
  アーキテクチャ(単調性制約、符号付き重み構造等)の追加を検討する |

## 4. 学習データ生成仕様

### 4.1 励起信号・データ生成の段階的方針

学習データはいきなり広帯域カバレッジを狙うのではなく、**PTP動作の再現に必要な範囲へ
段階的に拡張する**方針とする。各段階は前段階のデータで閉ループ検証(§6.2)を行い、
発散または精度不足が確認された場合にのみ次段階へ進む。

| 段階 | 信号種別 | 目的 | パラメータ目安 |
|---|---|---|---|
| Step1(基本) | PTP軌道フィードフォワードトルク(台形/S字速度指令をFB変換したトルク) | PTP動作に必要な最小限のトルク波形で学習データを構成する | 目標角度: 可動範囲内でランダムサンプリング |
| Step2(Step1で閉ループ発散・精度不足の場合) | 閉ループ実行時トルク波形(コントローラ+物理モデルでPTPを実行した際の実際のτ(t)) | Step1データに含まれない閉ループ特有の分布(covariate shift分)を直接補う | 目標角度・初期角度をStep1と同条件でランダムサンプリングし、コントローラ込みでシミュレーションして得たτ(t)を収集 |
| Step3(Step2でも改善しない場合) | PRBS/chirp/マルチサイン等の広帯域信号 | 未知の周波数帯・状態領域への汎化を狙った追加カバレッジ | PRBS切替周期: 制御周期の2〜20倍、chirp: 0.1〜10 Hz程度でスイープ |
| 補助(全段階共通) | ランダム初期値+ゼロ入力 | 自由振動応答(受動性検証用) | 初期角度・角速度をランダムに複数設定 |
| 補助(全段階共通) | ステップ応答 | 基本応答特性の取得(デバッグ・可視化用) | 振幅: τ_max の 20/50/80% |

- Step2のデータ生成には、その時点で学習済みのサロゲート(または初回はコントローラゲイン
  設計時に用いた物理モデル)を用いて閉ループシミュレーションを実行し、得られたτ(t)を
  そのまま追加学習データとして取り込む。これによりアブレーション実験A(PTP軌道データの
  みで学習)と、Step1のみで止めた場合の結果が対応する。
- Step3まで進んだ場合は、どの周波数帯・振幅域を追加したことで改善したかを記録し、
  Step1・Step2で不足していたカバレッジ領域を特定すること(要件F-07に対応)。

### 4.2 データカバレッジ要件

- 各段階(Step1/2/3)終了時点で、学習データの (θ, θ_dot, τ) 分布のヒストグラムを可視化し、
  閉ループ運転時に実際に発生する分布(コントローラのシミュレーション結果)を重ね合わせて
  **covariate shift** の有無を定量評価すること(要件F-02, F-07に対応)。この評価結果が
  次の段階へ進むか否かの判断材料となる。
- 外挿判定指標(例: k近傍距離、Mahalanobis距離)を導入し、閉ループ実行中にサロゲートが
  学習分布外に外れた時刻を検出できるようにする

### 4.3 データセット仕様

| 項目 | 仕様 |
|---|---|
| サンプリング周期 Ts | Ts_surr(サロゲート動作周期)と同一とする。8kHz(125μs)版と200Hz(5ms)版の
  両方を用意する場合は、8kHzで生成した高精度データを200Hzへデシメーションして200Hz版を
  作る(§6.4のZOH方針に従う。独立に200Hzで積分し直さない) |
| データ長 | 各シナリオ 10〜60秒、シナリオ数 50〜200本 |
| train/val/test分割 | シナリオ単位で 70% / 15% / 15% |
| 保存形式 | .mat(MATLAB側生成) → .npy/.csv(Python学習用に変換) |

## 5. 学習仕様

### 5.1 損失関数

1step予測誤差のみでの学習は閉ループ誤差蓄積に弱いため、以下を必須とする。

```
L = Σ_{n=1}^{N} w_n * || y_hat_{k+n} - y_{k+n} ||^2
```

- N: ロールアウトホライズン(段階的に増加させるスケジュールを推奨: 例 1→5→20→50 step)
- w_n: ホライズン内の重み(均等 または 時間減衰)
- Scheduled Sampling(学習初期は教師強制、徐々に自己回帰予測へ移行)の適用を検討する

### 5.2 学習設定(初期案)

| 項目 | 値 |
|---|---|
| オプティマイザ | Adam |
| 学習率 | 1e-3(スケジューラで減衰) |
| バッチサイズ | シナリオ単位ミニバッチ(例: 16シナリオ) |
| エポック数 | 200〜1000(検証損失で早期終了) |
| ロールアウトホライズン スケジュール | 1 → 5 → 20 → 50 step(要件5.1参照) |

## 6. 検証仕様

### 6.1 オープンループ検証(要件F-04, F-05)

| 項目 | 合格基準(目安、要チューニング) |
|---|---|
| 長時間自由ロールアウト | 学習データ範囲内の初期値から100秒(整定時間の10倍以上)発散なし |
| 平衡点線形化固有値 | 離散系ならスペクトル半径 < 1、連続系相当なら実部 < 0 |
| ゼロ入力エネルギー挙動 | τ=0でエネルギー(0.5*J*θ_dot^2 + mgl(1-cosθ)相当)が単調非増加 |
| 1step予測誤差 vs ロールアウト誤差 | ロールアウト誤差の増大が時間に対して指数的でないこと(準線形〜飽和) |
| 入出力方向整合性(局所ゲイン符号、M-04) | 学習・検証データの動作軌道上の全時刻で ∂ŷ_{k+1}/∂τ_k > 0(物理モデルと同符号)。
  符号反転が生じる領域があれば(θ, θ_dot, τ)の範囲として記録する |

### 6.2 閉ループ検証(要件F-06, F-08)

コントローラは既存資産(PID or 状態フィードバック)を流用し、以下比較を行う。

| 比較項目 | 内容 |
|---|---|
| PTP応答波形 | 物理モデル閉ループ vs サロゲート閉ループの θ(t) 重ね描き |
| 整定時間・オーバーシュート | 定量比較、許容誤差(例:整定時間±10%)を事前定義 |
| ゲイン感度 | コントローラゲインを小→大にスイープし、発散が生じるゲイン閾値を両モデルで比較 |
| 外乱・初期値ロバスト性 | 目標角度・初期角度をランダム化した多数試行での発散率(%) |

### 6.3 発散原因切り分け実験(要件F-07)

意図的に劣化条件を作り、発散再現性を確認するアブレーション実験。

| 実験 | 操作 | 想定される結果 |
|---|---|---|
| A. データカバレッジ制限 | PTP軌道データのみで学習(PRBS等を除外) | 閉ループで未学習領域に入り発散しやすい |
| B. 1step損失のみ学習 | ロールアウト損失を使わずN=1で学習 | オープンループロールアウトで誤差蓄積・発散 |
| C. 活性化関数変更 | tanh → ReLU | 外挿領域での状態増分が線形増大し発散しやすい |
| D. サンプリング周期不一致 | 学習時Tsと制御ループ周期をずらす | 実効ダイナミクスがずれ、位相遅れ的に不安定化 |
| E. ゲイン過大 | コントローラゲインを物理モデル用のまま高めに設定 | サロゲートのモデル化誤差が高ゲインで増幅され発散 |
| F. 入出力方向制約なし(標準構成) | **これは標準のStep1構成そのもの**(f_θに方向制約を
  課さないM-01スケール制約のみのMLP)で閉ループを実行し、M-04診断ログを監視する | 誤差
  補正トルクと逆方向の推論結果が生じる領域で、補正トルクがさらに増大する正の
  フィードバックループが発生し発散する |

各実験について、§6.1・6.2の指標がどう悪化するかを記録し、要件定義書の「発散原因」との
対応表を作成する。実験Fは他の実験(A〜E)のように標準構成から意図的に劣化させるもの
ではなく、**現時点の標準構成(方向制約なし)で観測される挙動そのもの**を指す。閉ループ
ログ(§7)上で、補正トルク τ_fb の符号と、その後の θ_hat の変化方向(Δθ_hat)の符号が
一致する時間帯が発散直前に持続的に現れるかを確認し、この仮説が支持された場合に
初めてM-04の方向制約付きアーキテクチャの追加検討へ進む。

### 6.4 サンプリング・離散化整合性(多重レート構成)

本システムは以下の多重レート構成を標準とする。

| 対象 | 周期 | 備考 |
|---|---|---|
| 物理モデル数値積分・コントローラ制御周期 | 8kHz(Ts_ctrl = 125μs) | 基本周期。全ての基準となる |
| NSSサロゲートモデル | 8kHz(Ts_ctrl と同一) または 200Hz(Ts_surr = 5ms、40:1デシメーション) | ダウンサンプル運用を許容 |

サロゲートを200Hzで運用する場合、8kHzで動くコントローラとの間に以下のインターフェースを
明示的に定義し、実装・シミュレーション両方で一致させること。

- **入力側(コントローラ→サロゲート)**: コントローラが8kHzで出力するτ(t)を、サロゲート
  更新タイミング(200Hz、40サンプルごと)でサンプルするか、直前40サンプルの平均を取るかを
  明記し、両モデル(物理・サロゲート)で同一の変換方式を用いる。デフォルトはゼロ次ホールド
  相当(更新タイミングでの瞬時値サンプル)とする。
- **出力側(サロゲート→コントローラ)**: サロゲートの出力θ_kは次の更新(5ms後)まで
  ゼロ次ホールドでコントローラに渡す。これにより、8kHz動作時には無い**最大5msの
  検出遅延**が閉ループに追加されることをコントローラゲイン設計・§6.2ゲイン感度評価で
  考慮すること。
- 学習データのサンプリング周期Tsは、学習対象とするサロゲートの動作周期(8kHzまたは
  200Hz)と必ず一致させる。8kHzの高精度データを200Hzへデシメーションして200Hz版の
  データセットを作る場合も、上記のτサンプリング方式(ZOH)をデータ生成時から一貫させる
  こと。
- アブレーション実験D(サンプリング周期不一致、§6.3)は、この標準構成(8kHz/200Hzの
  整合したデシメーション)から意図的に外れた場合(例: 学習時のτサンプリング方式と実行時
  の方式が異なる、非整数比のデシメーションを用いる等)を想定した実験であり、200Hz運用
  そのものを不整合として扱うものではない。

## 7. 閉ループ制御ループ仕様

| 項目 | 仕様 |
|---|---|
| コントローラ形式 | PID(角度フィードバック)または状態フィードバック(P+PD on θ, θ_dot) |
| 目標軌道 | PTP: 台形速度 or S字加減速による θ_ref(t) |
| 制御周期 | 8kHz(基本)。サロゲート使用時に200Hzへダウンサンプルする場合は§6.4の多重レート
  インターフェース(ZOH入出力、40:1)に従う |
| 飽和処理 | τ_max でクリップ(物理・サロゲート双方に同一処理を適用) |
| ログ項目 | θ, θ_dot, τ, θ_ref, サロゲート内部状態 x, 外挿判定指標(§4.2)、
  補正トルク τ_fb とΔθ_hat(サロゲート出力の1step変化量)の符号一致フラグ(M-04, §6.3実験F) |

## 8. MATLAB / Python インターフェース仕様

| 項目 | 仕様 |
|---|---|
| データ受け渡し | MATLAB: .mat出力 → Python: scipy.io.loadmatで読込 |
| 学習済みモデル | PyTorchで学習 → ONNX or TorchScriptにエクスポート |
| MATLAB側推論 | `Deep Learning Toolbox`の`importONNXNetwork`、または
  MATLAB Function BlockからPython実行環境を呼び出すS-function/co-simulation |
| Simulink結合 | サロゲートモデルをSimulink内でMATLAB Function/co-simブロックとして
  物理モデルと同一インターフェース(τ入力、θ出力)で差し替え可能にする |

## 9. 出力・レポート仕様(要件F-09)

- §6.1〜6.3の各指標を自動集計し、Markdown/HTMLレポートとして出力
- 物理 vs サロゲートの時系列比較図、固有値プロット、分布カバレッジ図を含める
- 6軸展開時に同一レポート様式を使えるよう、状態次元数に依存しないテンプレートとする

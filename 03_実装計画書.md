# 実装計画書:単振り子サロゲートモデル 原理検証システム

- 文書番号: PLAN-SURR-001
- 版数: Rev.1.0
- 対応文書: REQ-SURR-001(要件定義書)、SPEC-SURR-001(仕様書)

## 1. 実装方針

- MATLAB/Simulinkを物理モデル・閉ループ検証・可視化の基盤とし、NSS学習はPython(PyTorch)で
  行うハイブリッド構成とする(仕様書§8)
- Claude Codeでの実装を前提に、フェーズごとにリポジトリ構成・成果物を明確化し、
  各フェーズ完了時にテスト(検証スクリプト)を実行して次フェーズへ進む
- ディレクトリ構成(想定):

```
project-root/
├── matlab/
│   ├── plant_pendulum.m / .slx      # 物理モデル
│   ├── controller_pid.m / .slx      # コントローラ
│   ├── gen_training_data.m          # データ生成(励起信号)
│   ├── closed_loop_compare.m        # 閉ループ比較実行
│   └── export_utils.m               # .mat入出力ユーティリティ
├── python/
│   ├── data/                        # 変換済み学習データ
│   ├── models/
│   │   └── nss.py                   # NSSモデル定義
│   ├── train.py                     # 学習スクリプト(ロールアウト損失対応)
│   ├── eval_openloop.py             # オープンループ検証
│   ├── eval_stability.py            # 線形化・固有値解析
│   └── export_onnx.py               # ONNXエクスポート
├── experiments/
│   └── ablation_A_F/                # §6.3のアブレーション実験結果
├── reports/
│   └── report_template.md
└── docs/
    ├── 01_要件定義書.md
    ├── 02_仕様書.md
    └── 03_実装計画書.md
```

## 2. フェーズ構成とタスク

### Phase 0: 環境構築・共通基盤(所要目安: 0.5〜1日)

| タスク | 成果物 |
|---|---|
| Python環境構築(PyTorch, numpy, scipy, matplotlib) | requirements.txt / 環境検証スクリプト |
| MATLAB⇔Python間の .mat 相互変換ユーティリティ作成 | export_utils.m, io_utils.py |
| 乱数シード管理・実験管理(config, ログ出力)の枠組み構築 | config.yaml, experiment runner |

### Phase 1: 物理モデル構築(所要目安: 1日)

| タスク | 成果物 | 対応要件 |
|---|---|---|
| 単振り子運動方程式の実装(MATLAB関数 & Simulink)。制御周期8kHzで積分 | plant_pendulum.m/.slx | F-01 |
| パラメータ設定(J, b, mgl, τ_max)と6軸1関節相当への調整 | パラメータ設定ファイル | 制約条件(§8) |
| サロゲート動作周期(8kHz/200Hz)を切替可能にするデシメーション・ZOHユーティリティ実装 | resample_utils.m | 仕様書§6.4 |
| 基本応答(ステップ、自由振動)の目視確認 | 検証プロット | - |

**完了条件**: 物理モデル単体でエネルギー保存則・減衰挙動が物理的に妥当であることを確認。

### Phase 2: 学習データ生成(所要目安: 1〜1.5日、Step2/3実施時は追加で発生)

学習データは仕様書§4.1の段階的方針(Step1: PTP軌道フィードフォワードトルク →
Step2: 閉ループ実行時トルク波形 → Step3: chirp等広帯域信号)に従い生成する。
Step2・Step3はPhase5(閉ループ検証)でcovariate shift・発散が確認された場合にのみ
Phase2へ戻って追加実施する反復ループとなる。

| タスク | 成果物 | 対応要件 |
|---|---|---|
| Step1: PTP軌道生成(台形速度指令+フィードバック逆算トルク)によるデータ生成 | gen_training_data.m, ptp_trajectory.m | F-02 |
| Step2(必要時): 閉ループ実行時トルク波形の収集・追加(Phase5からのフィードバック) | gen_training_data_closedloop.m | F-02, F-07 |
| Step3(必要時): PRBS/chirp等広帯域信号の追加(Step2でも不足する場合) | gen_training_data_broadband.m | F-02 |
| データセット分割(train/val/test)とcovariate shift可視化 | dataset.mat, coverage_plot | 4.2節要件 |

**完了条件(Step1時点)**: (θ, θ_dot, τ)のヒストグラムがPTP軌道フィードフォワード相当の
範囲をカバーしていることを確認し、Phase3〜5へ進む。Phase5で発散・精度不足が判明した
場合はStep2、それでも不足する場合はStep3を追加実施し、該当箇所を再度Phase3〜5へ流す。

### Phase 3: NSSモデル実装・学習(所要目安: 2〜3日)

| タスク | 成果物 | 対応要件 |
|---|---|---|
| NSSモデル定義(状態遷移f_θ、出力g_φ)実装。出力形式(θ/θ_dot/θ_ddot/組み合わせ)を
  切替可能にする | models/nss.py | 仕様書§3.1 |
| Step1: 出力y_k=θ_kでの学習・閉ループ検証 | 学習済みモデル(角度出力版) | 仕様書§3.1 |
| Step2(Step1で精度・安定性不足の場合): 出力y_k=θ_dot_k(または+θ_ddot_k)へ切替え、
  数値積分によるθ再構成とドリフト評価を実装 | integrate_feedback.py, 学習済みモデル(角速度/角加速度出力版) | 仕様書§3.1 |
| ロールアウト損失・ホライズンスケジューリング実装 | train.py | 仕様書§5.1 |
| 基本学習(N=1固定)とロールアウト学習(N拡大)の2パターン学習・比較 | 学習済みモデル×2 | アブレーション実験Bの準備 |
| 学習曲線・検証誤差のロギング | tensorboard/ログ出力 | N-01再現性 |

**完了条件**: 検証データでの1step予測誤差が許容範囲内(具体値は初回学習結果を見て設定)。

**初回学習結果(2026-09-13、200Hzダウンサンプル、hidden=64x64、80エポック相当)**:

| モデル | 1step検証誤差 | 50stepロールアウト誤差(正規化MSE) |
|---|---|---|
| N=1固定(アブレーション実験Bのベースライン) | 0.0010 rad | 203.7(発散に近い蓄積) |
| ロールアウト学習(1→5→20→50) | 0.0477 rad | 0.0035 |

N=1固定モデルは1step誤差自体は極めて小さいが、50stepロールアウトでは誤差が桁違いに
蓄積し発散的挙動を示した。ロールアウト学習モデルは1step誤差はやや大きい(θ可動範囲
±1.4radに対し約3.4%)が、ロールアウト誤差は3桁小さく安定している。これは仕様書§5.1の
仮説(1step損失のみでは誤差蓄積に弱い)を裏付ける結果であり、アブレーション実験Bの
再現としてそのまま利用できる。Phase3の1step誤差の合格基準は「0.05 rad以下」を暫定値
として採用する(ロールアウト学習モデルがこの基準をほぼ満たすため)。

### Phase 4: オープンループ検証(所要目安: 1〜1.5日)

| タスク | 成果物 | 対応要件 |
|---|---|---|
| 長時間自由ロールアウトスクリプト実装 | eval_openloop.py | F-04 |
| 平衡点線形化・固有値解析実装 | eval_stability.py | F-05 |
| ゼロ入力エネルギー挙動チェック実装 | energy_check.py | 仕様書§6.1 |
| 入出力方向整合性(局所ゲイン符号 ∂ŷ_{k+1}/∂τ_k)チェック実装。動作軌道上の符号反転
  領域を検出・可視化(初期段階では診断のみ、モデルへの制約追加は行わない) | check_sign_consistency.py | 仕様書M-04 |

**完了条件**: 仕様書§6.1の合格基準を満たすモデルを1つ以上得る(満たさない場合はPhase3へ戻り
アブレーション実験A・Cを先取りして原因調査)。

**初回検証結果(2026-09-13、nss_step1_rollout.pt、theta0=0.8rad, tau=0, 100秒)**:

| 検証項目 | 結果 |
|---|---|
| 長時間自由ロールアウト(発散有無) | PASS: max\|theta_hat\|=0.93rad、発散なし |
| 平衡点(θ=0)固有値 | PASS: スペクトル半径0.9973 (<1) |
| 入出力方向整合性(M-04) | PASS: 検証データ軌道上22941点全てで符号反転なし |
| ゼロ入力エネルギー単調性 | **FAIL**: 100秒間でエネルギーが0まで減衰せず、
  θ≈0.62rad・エネルギー≈0.038Jのスプリアスな定常点に収束(6/19999stepでエネルギー増加) |

**FAIL原因の切り分け**: 固有値・符号整合性は共にPASSしており、θ=0近傍の局所安定性や
学習軌道上の受動性は問題ない。原因はモデルアーキテクチャ(アブレーション実験Cの活性化
関数)ではなく、**Phase2 Step1のデータ生成が仕様書§4.1の「補助(全段階共通): ランダム
初期値+ゼロ入力(自由振動)」信号を含めていなかったこと**と特定した。PTPフィードフォワード
データは静止区間でも重力補償トルク(tau_ff = mgl*sin(theta_end))を印加しており、
真にtau=0の学習サンプルがtheta=0近傍にしか存在しない。この結果、モデルはtheta=0から
離れた位置でのtau=0時の正しい減衰的動特性を学習できておらず、アブレーション実験Aの
データカバレッジ不足パターンに合致する。

**対応後の再検証結果(2026-09-13、gen_training_data.mに自由振動補助信号20本を追加、
計80シナリオで再学習、20エポック/stageは変更なし)**:

| 検証項目 | 対応前 | 対応後 |
|---|---|---|
| 長時間自由ロールアウト(発散有無) | PASS | PASS(max\|theta_hat\|=0.93rad) |
| 平衡点固有値 | PASS(0.9973) | PASS(0.9983) |
| ゼロ入力エネルギー単調性 | FAIL(スプリアス定常点θ≈0.62rad) | **FAIL(改善はしたがθ≈0.27radへの
  収束が残る。10/19999stepでエネルギー増加)** |
| M-04入出力方向整合性 | PASS(0/22941点) | **FAIL(1214/28106点=4.32%で符号反転、
  自由振動の減衰螺旋軌道上に集中)** |

補助信号追加によりスプリアス定常点はθ=0に近づいた(0.62rad→0.27rad)が完全には解消せず、
むしろ新たに(θ, θ_dot)空間の未検証領域(自由振動軌道)がカバーされたことで、そこに
潜在していたM-04符号反転(4.32%)が可視化された。これは「データを増やす前は検証すら
できていなかった弱点」であり、20エポック/stageでの学習収束が不十分である可能性が高い
(1step誤差は0.048→0.026radへ改善しているため、学習自体は進んでいる)。

**今後の対応方針(要判断)**: (a)エポック数・ネットワーク容量を増やして再学習し収束を
図る、(b)これをアブレーション実験F(M-04仮説)の支持材料とみなし、方向制約付き
アーキテクチャの検討に進む、(c)現状のまま許容してPhase5(閉ループ検証)へ進み実際の
閉ループ挙動で深刻度を再評価する、の3択を提示し、(a)を選択。

**(a)を試した結果(EPOCHS_PER_STAGE 20→60)、重大な悪化が判明したため60エポック版は
採用せず20エポック版へ復元した**:

| 検証項目 | 20エポック/stage | 60エポック/stage |
|---|---|---|
| 1step検証誤差 | 0.026 rad | 0.025 rad(ほぼ同じ) |
| 50stepロールアウトMSE(正規化、ウィンドウ内) | 0.0023 | 0.0010(改善して見える) |
| 100秒自由ロールアウト | PASS(θ収束0.93→0.27rad) | **FAIL(θ≈23.3radまで発散)** |
| 平衡点固有値 | PASS(スペクトル半径0.998) | **FAIL(スペクトル半径1.066、不安定)** |
| M-04符号整合性 | FAIL(4.32%反転) | **FAIL(94.57%反転、ほぼ全域で逆方向)** |

**重要な教訓**: 学習に使った検証指標(50stepウィンドウ内ロールアウトMSE)は60エポック版
の方が良好だったにもかかわらず、実際の長時間自由ロールアウト・平衡点安定性・M-04符号
整合性は壊滅的に悪化した。これは「訓練/検証で使うウィンドウ内ロールアウト損失だけでは、
それより長い時間スケール(閉ループの実運用時間)での安定性を保証しない」ことを示す
実例であり、まさにM-02(固有値解析)・M-04(符号整合性)を**学習損失とは独立した必須の
安全性診断**として要件化した理由(仕様書§3.3)を裏付ける。エポック数を闇雲に増やす
方向はリスクが高いため今後は避け、best-checkpoint選択(検証損失ではなくM-02/M-04を
含めた安定性指標での選択)や、方向制約付きアーキテクチャの検討を優先する。

**現状の結論**: 20エポック/stageのモデル(nss_step1_rollout.pt)を暫定採用し、
Phase5(閉ループ検証)へ進みつつ、M-04符号反転(4.32%)とゼロ入力エネルギー未収束を
既知のリスクとして注視する。

**M-04符号反転の原因調査(Issue #25)**: 符号反転点は正常点と比べ平均|θ̇|が16倍大きく
(1.03 vs 0.065 rad/s、自由振動の高速域に集中)、感度の大きさ(中央値)は約半分だった。
これを踏まえ「高速域のデータを厚くすれば改善するか」を検証するため、
gen_training_data.mに`n_free_vibration_highvel`引数を追加し(デフォルト0、後方互換)、
|θ̇0|=1.2〜2.0radに偏らせた自由振動シナリオを20本追加(計100シナリオ)して再学習した。
**結果: M-04符号反転は4.32%→4.63%とほぼ変化せず、他の指標もわずかに悪化した**。
「データが薄いから間違える」という仮説は否定され、「その領域では真の物理的感度自体が
小さく、値の誤差のみを見る損失関数がそこでの符号を気にする動機を持たない」という
仮説の方が支持される結果となった。これは**データカバレッジの問題ではなく損失関数設計
そのものに起因する構造的な限界**であることを示唆し、Ablation A的アプローチ(データ拡充)
では解決しないと結論づける。標準モデル・データセットは80シナリオ版に復元済み。

### Phase 5: MATLAB/Simulink結合・閉ループ検証(所要目安: 2日)

| タスク | 成果物 | 対応要件 |
|---|---|---|
| サロゲートモデルのONNXエクスポート、MATLABへの取り込み | export_onnx.py, surrogate_block.slx | F-03, F-06 |
| 物理モデル+コントローラ、サロゲート+コントローラの並列閉ループ構築 | closed_loop_compare.m/.slx | F-06, F-08 |
| PTP応答比較、ゲインスイープ、ロバスト性試行の自動実行 | 比較結果一式 | 仕様書§6.2 |

**完了条件**: 少なくとも1組のコントローラゲインで、サロゲート閉ループが物理モデル閉ループと
整合する(発散しない)PTP応答を得る。発散・精度不足が確認された場合は、まずPhase2の
Step2(閉ループ実行時トルク波形の追加)を実施して再学習・再検証し、それでも改善しない
場合にStep3(広帯域信号追加)を検討する(仕様書§4.1)。

**実施結果(2026-09-14)**:

- ONNXエクスポートは、このMATLAB環境に「Deep Learning Toolbox Converter for ONNX Model
  Format」が未インストールのため断念し、代替として重み(f_theta各層のW/b、g_φ、
  正規化統計量)を.matへエクスポートし、MATLAB関数(nss_surrogate.m)でネイティブに
  順伝播を実装する方式を採用(仕様書§8が明示する代替手段)。Python側出力との数値一致を
  確認済み(export_weights.py, nss_surrogate.m)。
- PDコントローラ(角度誤差Kp+角度微分誤差Kd、θ測定のみから両方を算出し物理・サロゲート
  で条件を揃えた)による閉ループを実装(closed_loop_compare.m)。サロゲートは200Hz
  ZOH(仕様書§6.4)で運用。

**ゲインスイープ結果(Kp=0.35固定, target=0.8rad, T=8s)**:

| Kd | 物理ループ | サロゲートループ |
|---|---|---|
| 0.00〜0.03 | 安定(max\|θ\|≈0.65〜0.73rad) | **安定**(max\|θ\|≈0.61rad) |
| 0.05〜0.09 | 安定(max\|θ\|≈0.63〜0.65rad) | **発散**(max\|θ\|≈33.2rad) |

物理ループはKd=0〜0.09の全域で安定(発散なし)である一方、サロゲートループはKd=0.03と
0.05の間に明確な発散閾値を持つ。Kd=0(P制御のみ)ではサロゲート閉ループは物理ループと
ほぼ整合するPTP応答が得られ、**Phase5の完了条件を満たすゲイン(Kp=0.35, Kd=0.02)**を
確認した(reports/phase5_closed_loop_comparison.png)。

**発散原因の切り分け**: Kd項は「θ測定値をサロゲート更新周期(5ms)で有限差分微分した値」
を使っており、y_surrの1step変化量Δyを5ms(=1/200Hz)で割るため**200倍の増幅**がかかる。
サロゲートの1step予測誤差(M-04符号反転4.32%由来の微小なブレを含む)がこの増幅を経て
Kd項に大きなノイズとして現れ、飽和(±3N·m)を伴う制御指令の振動 → 発散、という経路を
実際に観測した(k=41〜361付近でtau_cmdが±3N·m付近を往復しながらθが単調増加する挙動を
デバッグ出力で確認)。これは要件定義書§2の発散原因候補「4. 離散化・サンプリング周期の
不整合」と「5. 制御器側のゲイン設計とサロゲート特性のミスマッチ」の複合(低速サロゲート
出力に対して高ゲイン微分制御を組み合わせたことによる離散化ノイズ増幅)に該当する、
6軸展開でも再現しうる重要な知見である。

**追加対応(Issue #19、2026-09-14): θ̇フィードバックを内部潜在状態から取得する解決策**

上記の発散原因(有限差分によるθ̇推定の200倍ノイズ増幅)に対し、仕様書§3.1のStep2
(角速度出力への切り替え)の最小実装として、サロゲートが内部で既に計算している潜在状態
の第2成分(x_surr(2))をθ̇フィードバックとして直接使う方式を検証した。**再学習は不要**
(既存モデルの出力の使い方を変えるだけ)。

| 条件 | 有限差分方式(従来) | 内部状態方式(改善) |
|---|---|---|
| Kd=0.09(発散していたゲイン) | 発散(θ≈33rad) | 安定(θ≈0.85rad) |
| Kd=0.30〜1.00 | (0.09で既に発散) | 全て安定(θ≈0.85〜5.6rad、ゲインに応じ増加するが発散せず) |
| runtime_ratio=10, 20(実験Dで発散) | 発散 | 全て安定 |

この変更のみで、#12(ゲイン過大)・実験D(サンプリング周期不整合)の両方の発散が解消した。
これは離散化ノイズ増幅という「原因」に対する直接的な「対策」であり、根本原因の特定
(本Phase5)と対策の実証(本追記)がセットで完了したことを意味する。

**残る既知リスク**: (1)ゼロ入力エネルギー未収束(θ≈0.27radのスプリアス平衡点、#10)は
本対策では解消しない別問題として残存。(2)内部潜在状態のθ̇チャンネル自体にM-04相当の
符号反転が混入していないかは未検証。(3)本対策はθ̇を「教師なしで学習された副産物」と
して流用しており、θ̇に対する明示的な教師信号(真のθ̇)で学習し直す(仕様書§3.1
Step2の本来の実装)方が、より信頼性の高い解決になる可能性が高い。

### Phase 6: 発散原因アブレーション実験(所要目安: 2〜3日)

| タスク | 成果物 | 対応要件 |
|---|---|---|
| 実験A〜E(仕様書§6.3)の個別実行・結果記録。実験F(入出力方向制約なし)は標準構成
  そのものであるため、Phase3〜5で得たログをM-04診断で分析することで実施済みとする | experiments/ablation_A_F/ | F-07 |
| 各実験でどの指標(§6.1, 6.2)が悪化するかの対応表作成 | 発散原因対応表 | 要件定義書§7-3 |
| 外挿判定指標(k近傍距離等)の閉ループ組み込みと発散予兆検出の評価 | extrapolation_monitor.py | 仕様書§4.2 |

**完了条件**: 実際に観測されていた発散挙動を、アブレーション実験のいずれかで再現できる。

**実施結果(2026-09-14)**: 実験A・B・E・Fは、それぞれPhase2(#10)・Phase3(#7)・Phase5(#12)
で既に実質的に実施済み。本Phaseでは残るC・Dを実施し、発散原因対応表を完成させた。

**実験C(活性化関数変更、tanh→ReLU + M-01スケール制約の撤廃)**: 自由ロールアウト
(τ=0、100秒)を学習範囲内(θ₀=0.8rad)・学習範囲外(θ₀=2.5rad、カバレッジの約1.8倍)の
両方で実施したが、**いずれも発散しなかった**(max|θ|=0.85rad, 2.53rad、平衡点固有値
スペクトル半径0.935<1)。仮説(ReLUは外挿領域で線形増大し発散しやすい)は本データセット・
本テスト条件では再現されなかった。これは仮説が誤りというより、①ゼロ入力(自由振動)という
穏やかな条件でしか試していない、②Issue #10で追加した自由振動補助データが偶然この
ネットワークの汎化を助けた可能性がある、ため。閉ループ(トルク駆動)条件での再検証は
未実施(follow-up)。

**実験D(サンプリング周期不整合)**: `closed_loop_compare.m`に`runtime_ratio`引数を追加し、
学習時の前提(200Hz, ratio=40)とは異なる頻度でサロゲートを呼び出す実験を実施
(Kp=0.35, Kd=0.02、Phase5で安定確認済みのゲイン)。

| runtime_ratio(学習時=40) | 実効更新周期 | サロゲートループ |
|---|---|---|
| 40(整合) | 5.0ms | 安定 |
| 80 | 10.0ms(遅い) | 安定 |
| 20 | 2.5ms(速い) | **発散** |
| 10 | 1.25ms(速い) | **発散** |

学習時より高頻度にサロゲートを呼び出す(実効周期を短くする)と発散し、低頻度にすると
安定なままだった。原因はPhase5と同じ機構(θ̇推定の有限差分をより短い時間間隔で割るほど
ノイズ増幅率が上がる)であり、**実験D(離散化不整合)と実験E(ゲイン過大)は本モデル
アーキテクチャにおいて同一の物理的機構(有限差分微分のノイズ増幅)を共有する**ことが
判明した。これは6軸展開時にも一般化できる重要な知見(離散化とゲインは独立した問題では
なく、密結合したリスクとして評価すべき)。

**外挿判定指標(k近傍距離、extrapolation_monitor.py)**: 学習データ(train split)を
参照点とするk=5近傍距離を実装し、検証データ(val split)の99パーセンタイル距離を
しきい値として校正した。動作確認として学習範囲外(θ₀=2.5rad)の自由ロールアウトに
適用したところ、軌道の最初の約1秒間(191/20000ステップ)が正しく外挿と判定された。
一方、Phase4で問題視した学習範囲内(θ₀=0.8rad)の自由ロールアウト(スプリアス定常点
θ≈0.27radへの収束)は**0%が外挿と判定された(0/20000ステップ)**。これは重要な結果で、
**残存するゼロ入力エネルギー未収束の問題は、データカバレッジ不足(外挿)ではなく、
学習範囲内でのモデルフィッティング誤差である**ことを示す。Issue #10で「データを増やせば
直る」と考えていた部分に対する反証であり、今後の対応は追加データではなく学習手法・
アーキテクチャ側(M-04方向制約等)を優先すべきという結論を補強する。

**発散原因対応表(要件定義書§2の候補1〜5との対応)**:

| 実験 | 操作 | 悪化した指標 | 対応する発散原因候補(要件定義書§2) |
|---|---|---|---|
| A(データカバレッジ、#10) | 自由振動補助信号の有無 | ゼロ入力エネルギー(θ≈0.62→0.27rad)、M-04符号反転(0%→4.32%、可視化) | ①学習データ設計不良 |
| B(1step損失のみ、#7) | ロールアウト損失を使わずN=1固定学習 | 50stepロールアウトMSE(0.002→203〜277、発散的) | ③学習方法の問題 |
| C(活性化関数、本Phase) | tanh→ReLU、M-01制約撤廃 | なし(本テスト条件では未再現) | ②力学的不安定性(未確認、follow-up要) |
| D(サンプリング周期、本Phase) | runtime_ratioを学習時と不一致に | 高頻度側(ratio=10,20)でθ発散(33rad) | ④離散化・サンプリング周期の不整合 |
| E(ゲイン過大、#12) | Kdを0.03→0.05以上に増加 | θ発散(33rad)、tau振動・飽和 | ⑤制御器ゲインとサロゲート特性のミスマッチ |
| F(方向制約なし、#10) | 標準構成(M-04制約なし)そのもの | M-04符号反転(4.32%、自由振動軌道上) | ②力学的不安定性・受動性の破れ |

D・Eが同一機構(有限差分微分のノイズ増幅)を共有していたことから、①④⑤は「学習時と
実行時のレート・ゲイン設計が噛み合っていない」という一つの大きな原因群として扱うのが
6軸展開時のチェックリストとして妥当と考えられる(Phase7で一般化)。

### Phase 7: 要件一般化・レポーティング(所要目安: 1日)

| タスク | 成果物 | 対応要件 |
|---|---|---|
| Phase0〜6の知見をまとめた「閉ループ組み込みチェックリスト」作成 | checklist_closed_loop.md | F-10 |
| 自動レポート生成テンプレート整備(6軸展開でも再利用可能な形) | reports/report_template.md | F-09 |
| 6軸ロボットへの適用時の差分・拡張ポイント整理(Stage3/4への申し送り) | handoff_notes.md | 要件定義書§9 |

## 3. マイルストーン・スケジュール(目安)

| マイルストーン | 内容 | 目安終了時点 |
|---|---|---|
| M1 | 物理モデル・データ生成完了(Phase0-2) | 開始後 3日 |
| M2 | NSS学習・オープンループ検証で安定モデル取得(Phase3-4) | 開始後 6.5日 |
| M3 | 閉ループ検証で物理モデルと整合する応答取得(Phase5) | 開始後 8.5日 |
| M4 | 発散原因の特定・再現(Phase6) | 開始後 11.5日 |
| M5 | 要件一般化・6軸展開準備完了(Phase7) | 開始後 12.5日 |

※日数は1人で連続作業した場合の目安。並行作業やレビュー待ちで前後する想定。

## 4. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| 単振り子で発散を再現できない(6軸固有の問題の可能性) | 原因特定が進まない | Stage3(2リンク)へ前倒しで拡張、結合慣性・コリオリ項起因を疑う |
| ロールアウト学習が計算コスト高で収束しない | スケジュール遅延 | ホライズンを小さく開始し段階的に拡大、バッチサイズ調整 |
| MATLAB⇔Python連携(ONNX等)でI/F不整合 | Phase5で手戻り | Phase0で最小構成の疎通テストを先行実施 |
| パラメータ(J, b, mgl等)が6軸1関節と乖離し知見が転用不可 | 一般化失敗 | 6軸モデルの代表関節のパラメータレンジを事前ヒアリングし設定 |
| コントローラゲイン設計が物理モデル用のままでサロゲートに不適合 | 閉ループ比較が公平でない | ゲインスイープ実験(仕様書§6.2)で感度を可視化し、公平な比較点を選定 |

## 5. 必要なツール・環境

| 種別 | ツール |
|---|---|
| 物理モデル・制御 | MATLAB, Simulink, Control System Toolbox |
| 学習 | Python 3.x, PyTorch, numpy, scipy, matplotlib |
| モデル変換 | ONNX, MATLAB Deep Learning Toolbox(importONNXNetwork) |
| 実験管理 | Claude Code(実装・実行)、Git(バージョン管理)、任意でMLflow/TensorBoard |

## 6. Claude Codeでの進め方(補足)

- 本計画のPhase単位でタスクを区切り、各Phase完了時に「完了条件」を満たすかを検証スクリプトで
  確認してから次Phaseへ進める運用とする
- 仕様書(02_仕様書.md)の数式・パラメータ・合格基準をそのまま実装のSingle Source of Truthとして参照する
- アブレーション実験(Phase6)は独立して並列実行可能なため、Phase5完了後にタスクを分割して
  同時に走らせることでスケジュール短縮が可能

# 6軸ロボットへの適用時の差分・拡張ポイント整理

- 文書番号: HANDOFF-SURR-001
- 版数: Rev.1.0
- 対応: 要件定義書REQ-SURR-001 §9(段階的検証アプローチ)
- 前提: 本文書は単振り子(Stage2)PoCの完了時点での申し送りであり、Stage3(2リンク平面
  マニピュレータ)を経てStage4(6軸ロボット)へ適用する際の参照資料とする。

## 1. そのまま再利用できるもの

- **多重レート・ZOHインターフェース設計**(仕様書§6.4、matlab/resample_utils.m):
  制御周期(8kHz)とサロゲート動作周期(200Hz)の分離は状態次元数に依存しない。ただし
  6軸では関節ごとに独立したセンサ・アクチュエータ遅延がある可能性があり、全関節で
  同一のダウンサンプル比を使ってよいかは要確認。
- **段階的データ生成方針**(仕様書§4.1、Step1→2→3): PTPフィードフォワード→閉ループ
  実行時トルク→広帯域信号、という順序と、「まず補助信号(ゼロ入力自由振動)を含める」
  という教訓(Issue #10)はそのまま適用できる。6軸では自由振動が関節間で結合する
  (2リンク以上ではコリオリ項・結合慣性が働く)ため、「各関節を単独で動かすゼロ入力
  試験」と「全関節同時のゼロ入力試験」の両方が必要になる可能性が高い(Stage3で要検証)。
- **診断指標一式**(M-02固有値解析、M-04符号整合性、外挿判定k近傍距離): いずれも
  状態次元数nに依存しないアルゴリズム(固有値はn×n行列、k近傍距離は次元非依存)。
  ただし6軸(n_x≥12程度)ではk近傍距離が次元の呪いの影響を受けやすくなるため、
  主成分分析等での次元圧縮を検討した方がよい。
- **ロールアウト損失によるホライズンスケジュール学習**(仕様書§5.1): Ablation Bの
  結果(1step損失のみでは閉ループで使い物にならない)は、システムの複雑さに関わらず
  一般的に成立する結論であり、6軸でも標準の学習方式として採用すべき。
- **閉ループ組み込みチェックリスト**(04_閉ループ組み込みチェックリスト.md): 6軸の
  代表関節のレビューにそのまま使える設計にしてある。

## 2. Stage3(2リンク)で追加検証が必要なもの

- **結合慣性・コリオリ項**: 単振り3は1自由度のため、関節間干渉に起因する発散は原理的に
  検証できていない(要件定義書§3.2スコープ外)。Stage3では、ある関節への入力トルクが
  他関節の状態に与える影響(∂θ_j_next/∂τ_i, i≠j)についてもM-04と同様の符号整合性
  チェックを拡張する必要がある。
- **ゲイン設計とサンプリングレートの結合**(Issue #12, #15の教訓): 単振り子では
  「有限差分によるθ̇推定のノイズがサロゲート更新周期の逆数で増幅される」という単純な
  機構だったが、2リンク以上では速度推定の結合(ヤコビアンを介した座標変換等)が絡む
  ため、同じ機構がどう現れるかは未知数。Stage3で同様のゲインスイープ・周期スイープ
  実験を再実施し、機構が同一かどうかを確認すること。

## 3. Stage4(6軸)で新たに考慮すべき事項

- **状態次元数の増大とゲインスイープの組み合わせ爆発**: 6軸では独立ゲインが6関節分
  (最低12パラメータ、Kp・Kd各6)あり、単純な1次元スイープでは発散境界を特定できない。
  感度解析(ヤコビアン)やベイズ最適化等での効率的な探索を検討する必要がある。
- **学習データ生成の計算コスト**: 6軸のPTP動作は関節空間の軌道計画が単軸よりも複雑
  (台形速度則の単純な適用では済まない)。既存のロボット軌道生成ツール
  (Robotics System Toolbox等)との連携を検討する。
- **訓練指標だけでモデルを選ばない、という教訓の運用面での徹底**(Issue #10):
  6軸では学習の反復回数が単振り子よりずっと多くなることが予想されるため、「エポック数を
  増やしたら訓練損失は改善したが実運用性能は悪化した」という事故が起きやすい。CI的な
  自動チェック(M-02/M-04/長時間ロールアウトを毎回自動実行し、悪化していたら学習を
  reject する)を早期に整備すべき。
- **外挿判定の次元削減**: 前述の通り、k近傍距離は高次元では機能が弱まるため、6軸では
  代替手法(オートエンコーダの再構成誤差、Mahalanobis距離+PCA等)を検討する。

## 4. 未解決のまま持ち越す論点

- **(構造的解決策あり・要組み合わせ検証) M-04(入出力方向整合性)の符号反転**は、
  Issue #27でcontrol-affine構造(x_{k+1}=x_k+tanh(f_drift(x_k))+B・u_k、状態と
  入力を分離)を導入することで4.32%→0.00%へ完全に解消することを確認した。単振り子は
  入力に関して厳密に線形(θ_ddot=...+τ/J)という物理的性質を持つため、この構造が
  よく適合した。1step精度・ゼロ入力エネルギーも同時に改善しており、トレードオフの
  ない結果が得られている。**ただしIssue #27はStep1(θのみ出力)構成での検証であり、
  Issue #23のStep2(θ̇明示教師)や閉ループでの実証とは未統合**。6軸展開前に、
  (a)control-affine構造とStep2出力を組み合わせたモデルでの閉ループ検証、
  (b)6軸ロボットが実際にどの程度「入力線形」に近いか(モータ・減速機の非線形性等)の
  確認、を行うべき。6軸ロボットの動力学は一般に τ = M(θ)θ_ddot + C(θ,θ_dot)θ_dot +
  g(θ) の形(質量行列M(θ)は状態依存)であり、単振り子ほど単純に入力線形ではない点に
  注意——control-affine構造自体は一般化できる(質量行列の逆行列をf_driftとは別に
  状態依存のB(θ)として学習する拡張も考えられる)が、単純な定数Bでは6軸には不十分な
  可能性が高い。
- **実験C(活性化関数)の陰性結果**: ゼロ入力条件でReLU+無制約モデルが発散しなかった
  ことは、tanh制約が不要であることを意味しない。閉ループ(トルク駆動)条件での再検証が
  必要(Issue #15コメント参照)。
- **(解決済み・要フォロー) 内部潜在状態を物理量として読み替えて使う問題**(Issue #21)は
  Issue #23でStep2(g_φの出力を[θ,θ̇]の2次元にし、θ̇も真値で教師)を実装することで
  対応した。閉ループ発散は内部状態流用版(#19)よりさらに広いゲイン範囲で解消し、
  ゼロ入力エネルギー収束・平衡点安定性も改善した。**ただしM-04符号反転は4.32%→6.86%へ
  悪化しており、閉ループ安定性の改善とM-04リスクの低減は別軸のトレードオフであることが
  判明した**。6軸展開時は、θ̇出力を追加する場合でも必ずM-04診断を同時に再実行し、
  片方の指標の改善だけで「解決した」と判断しないこと。運動学的制約を組み込んだ構成
  (M-03のStructured NSS)は、両方を同時に改善する可能性があるが本PoCでは未実装・
  未検証のまま持ち越し。

# 要件定義書:サロゲートモデル閉ループ制御 原理検証(単振り子)

- 文書番号: REQ-SURR-001
- 版数: Rev.1.0
- 対象プロジェクト: 6軸ロボット マニピュレーターのサロゲートモデル化

## 1. 目的

6軸ロボットのマニピュレーター(トルク入力→角度出力)をNSS(Neural State Space)でサロゲート化し、
制御器を含む閉ループシミュレーションに組み込む際に発生する「発散」問題の原因を切り分け、
再発を防止するための要件・検証プロセスを確立する。

6軸(状態次元が大きい)でいきなり原因調査すると、以下のどれが原因か切り分けが困難:

1. 学習データの設計不良(カバレッジ不足、covariate shift)
2. NSSモデル自体の力学的不安定性(受動性・エネルギーの破れ)
3. 学習方法の問題(1step予測のみで学習し誤差蓄積に弱い)
4. 離散化・サンプリング周期の不整合
5. 制御器側のゲイン設計とサロゲート特性のミスマッチ

そのため、本プロジェクトでは**単振り子(1リンク、トルク入力→角度出力)**を題材とした
原理検証(PoC)を先行実施し、6軸ロボットへ適用可能な「サロゲートモデルを閉ループに
組み込む際のチェックリスト・設計要件」を確立することを目的とする。

## 2. 背景・課題

- 対象: 6軸ロボットマニピュレーター(トルク入力、角度出力)、MATLAB上に物理モデル構築済み
- 現状: 学習済みNSSサロゲートモデルを制御器込みの閉ループに投入すると、PTP(Point-to-Point)
  動作時に発散するような挙動を示す
- アーキテクチャ: NSS(Neural State Space)
- 課題: 発散の原因が「モデル」「学習」「データ」「制御系との組み合わせ」のいずれに起因するか未特定

## 3. スコープ

### 3.1 スコープ内

- 単振り子(1自由度、非線形、トルク入力→角度出力)を用いた原理検証
- NSSサロゲートモデルの学習・オープンループ検証・閉ループ検証の一連のプロセス構築
- 発散原因の切り分けに必要な評価指標・検証手順の定義
- 6軸ロボットへの適用を見据えた要件・チェックリストの一般化

### 3.2 スコープ外(本フェーズでは扱わない)

- 6軸ロボットの実データを用いた検証(次フェーズ)
- 2リンク以上の多自由度系での結合慣性・コリオリ項の検証(Stage3として別途計画)
- 実機ロボットへの実装・実機検証
- NSS以外のサロゲートアーキテクチャ(RNN, Transformer等)との比較検討

## 4. 用語定義

| 用語 | 定義 |
|---|---|
| NSS | Neural State Space。状態空間モデルの状態遷移関数・出力関数をニューラルネットで近似するアーキテクチャ |
| サロゲートモデル | 物理モデル(MATLAB/Simulink等)の入出力関係を模擬する学習済みモデル |
| PTP動作 | Point-to-Point動作。始点から終点まで軌道を規定せず到達させる動作様式 |
| 閉ループ発散 | サロゲートモデルを制御ループに組み込んだ際に、状態量が発散・振動的に増大する現象 |
| covariate shift | 学習データの分布と実運用(閉ループ)時に入力される状態分布が乖離すること |
| ロールアウト | モデルを初期状態から自己回帰的に多段先まで予測させること |
| 受動性 | 系がエネルギーを自ら生成しない(入力エネルギー以上を出力しない)性質 |

## 5. 機能要件

| ID | 要件 | 優先度 |
|---|---|---|
| F-01 | 単振り子の物理モデル(運動方程式ベース)をMATLAB/Simulinkで構築できること | 必須 |
| F-02 | 物理モデルに対し、PTP動作を含む多様な励起信号で学習用データを生成できること。生成はPTP軌道フィードフォワードトルクを基本とし、閉ループ検証で不足が判明した場合に閉ループ実行時トルク波形、次いでchirp等の広帯域信号へ段階的に拡張する方式とする(詳細は仕様書§4.1) | 必須 |
| F-03 | NSS構造のサロゲートモデルを学習し、物理モデルと同一I/O(トルク入力、角度・角速度・角加速度およびその組み合わせのいずれかを出力)を持つこと。出力は角度を基本とし、閉ループ検証で精度・安定性が不十分な場合に角速度・角加速度出力へ切り替え、数値積分してフィードバックに用いる段階的方式とする(詳細は仕様書§3.1) | 必須 |
| F-04 | サロゲートモデル単体でのオープンループ長時間ロールアウト検証ができること | 必須 |
| F-05 | サロゲートモデルの平衡点近傍での線形化・安定性(固有値)解析ができること | 必須 |
| F-06 | サロゲートモデルを既存コントローラ(PID等)と結合した閉ループシミュレーションができること | 必須 |
| F-07 | 発散が生じた場合に、原因(データ/モデル/学習/離散化/制御)を切り分けるための診断指標を出力できること。特に、補正トルクの符号とサロゲート推論結果の変化方向の符号が一致し続ける(正のフィードバックループ化する)現象を検出できること(有力仮説、仕様書M-04・§6.3実験F) | 必須 |
| F-08 | 物理モデルとサロゲートモデルの閉ループ応答を同一条件で比較できること | 必須 |
| F-09 | 検証結果(安定性・精度)をレポートとして可視化・出力できること | 推奨 |
| F-10 | 本検証で確立した要件・チェックリストを6軸モデルに適用可能な形で一般化・文書化できること | 必須 |

## 6. 非機能要件

| ID | 要件 | 内容 |
|---|---|---|
| N-01 | 再現性 | 乱数シードを固定し、学習・評価が再現可能であること |
| N-02 | 計算コスト | 単振り子モデルでの1回の学習・検証サイクルが実用的な時間(目安:数十分以内)で完了すること |
| N-03 | 一般化容易性 | 検証コード・指標が2リンク・6軸へ拡張しやすい構造であること(状態次元数をパラメータ化) |
| N-04 | 可搬性 | MATLAB環境を基本としつつ、学習部分はPython(PyTorch等)との連携も許容する |
| N-05 | 追跡可能性 | 各検証ステップの入力(データセット・ハイパーパラメータ)と出力(モデル・指標)を紐付けて保存すること |

## 7. 成功基準・受け入れ条件

以下をすべて満たした時点で、本PoCフェーズの完了(6軸展開フェーズへの移行可)とする。

1. **オープンループ安定性**: 学習済みサロゲートモデルが、学習データ範囲内の初期値・入力から
   PTP動作相当の時間長(目安:整定時間の5〜10倍)自由運転させても発散しないこと
2. **閉ループ再現性**: サロゲートモデル+コントローラの閉ループPTP応答が、物理モデル+同一
   コントローラの応答と比較して、整定時間・オーバーシュートの誤差が定義した許容範囲内であること
3. **発散原因の特定**: 意図的に発散を再現させる実験(例:データカバレッジを狭める、1step損失のみで
   学習する等)を行い、要件5.でリストした要因のどれが発散を引き起こすかを実証できること
4. **要件の一般化**: 上記知見を基に、閉ループ組み込み時のチェックリスト(要件定義)が文書化され、
   6軸モデルのレビューに使用できる形になっていること

## 8. 制約条件

- 単振り子モデルは6軸ロボットの1関節相当として、重力項・粘性摩擦項を含む非線形1自由度系とする
- NSSのアーキテクチャ(層数、状態次元、活性化関数等)は6軸で採用予定のものと基本方針を揃える
- 学習フレームワークは既存の6軸開発環境との連携を考慮し、Python(PyTorch)を主、MATLABはI/F・
  物理モデル・可視化を担当する構成とする(実装計画書で詳細化)
- 物理モデル・コントローラの制御周期は8kHzを基本とする。NSSサロゲートモデルは実行負荷
  低減のため200Hzへダウンサンプルして運用することを許容し、その場合の8kHz⇔200Hz間の
  入出力インターフェース(ゼロ次ホールド、40:1)を明確に定義すること(仕様書§6.4)

## 9. 段階的検証アプローチとの関係

本要件定義書はStage2(単振り子)を対象とする。将来的にStage3(2リンク平面マニピュレータ、
結合慣性・コリオリ項を含む)、Stage4(6軸ロボット)へと同一の要件フレームワークを適用し、
段階的に複雑度を上げて検証範囲を拡大する。

| Stage | 対象 | 目的 |
|---|---|---|
| Stage1 | 質量-バネ-ダンパ(線形1自由度) | 学習パイプライン・NSS実装の基礎検証(解析解と比較) |
| Stage2(本文書) | 単振り子(非線形1リンク) | 閉ループ発散原因の切り分け、要件の確立 |
| Stage3 | 2リンク平面マニピュレータ | 結合慣性・コリオリ項がある場合の要件拡張 |
| Stage4 | 6軸ロボット | 確立した要件の本適用 |

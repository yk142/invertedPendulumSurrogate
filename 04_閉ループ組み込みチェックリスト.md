# 閉ループ組み込みチェックリスト:NSSサロゲートモデル

- 文書番号: CHECKLIST-SURR-001
- 版数: Rev.1.0
- 対応: 要件定義書REQ-SURR-001 F-10、実装計画書Phase7
- 適用範囲: NSS(Neural State Space)サロゲートモデルを制御ループに組み込む際のレビュー。
  単振り子(Stage2)PoCで得た知見を一般化したもので、状態次元数に依存しない。
  6軸ロボット(Stage4)への適用時は、各項目を関節数分繰り返すか、代表関節で確認する。

各項目には、単振り子PoCで実際に確認した根拠(Issue番号・スクリプト)を付記する。
「なぜこの項目が必要か」が分かるようにするためで、6軸展開時にチェックが形骸化しない
ようにする狙いがある。

## 1. データ設計

- [ ] 学習データは、想定する運用パターン(PTP動作等)のフィードフォワード信号だけでなく、
      **代表的な"静止"条件(ゼロ入力での自由応答)を含むか**。
      根拠: PTPフィードフォワードのみで学習したモデルは、静止区間でも重力補償トルクが
      印加され続けるため、真にゼロ入力時の減衰特性を学習できていなかった(Issue #10)。
- [ ] 学習データのカバレッジ(状態・入力の分布)を可視化し、閉ループ実行時に実際に訪れる
      分布と比較したか(covariate shift確認、仕様書§4.2)。
- [ ] 外挿判定指標(k近傍距離等)を用意し、閉ループ実行中にサロゲートが学習分布外に
      出た時刻を検出できるようにしたか。
      根拠: extrapolation_monitor.py(#15)。範囲外投入(θ₀=2.5rad)は正しく検出できる
      ことを確認済み。**ただし、モデルの不具合が全て「外挿」で説明できるとは限らない**
      — 単振り子PoCで残存した問題(後述4.)は学習範囲内のモデルフィッティング誤差であり、
      外挿判定では検出されなかった(0/20000ステップ)。外挿判定は必要条件であって
      十分条件ではないことに注意。

## 2. モデル・学習方法

- [ ] 学習損失は1step予測誤差のみでなく、**複数stepのロールアウト損失**(ホライズンを
      段階的に増やすスケジュール推奨)を使っているか。
      根拠: 1step誤差0.0010radという極めて高精度なモデルが、50stepロールアウトでは
      正規化MSE 203〜277という発散的挙動を示した(Issue #7)。1step精度は閉ループでの
      安全性を全く保証しない。
- [ ] **訓練・検証で使う損失(ウィンドウ内ロールアウトMSE等)の改善を、長時間安定性の
      改善だと安易に解釈していないか**。エポック数やモデル容量を増やす場合は、必ず
      以下の独立した診断で再検証すること。
      根拠: エポック数を20→60に増やしたところ、ウィンドウ内ロールアウトMSEは改善した
      (0.0023→0.0010)にもかかわらず、100秒自由ロールアウトは発散(θ≈23rad)、平衡点
      固有値は不安定化(スペクトル半径0.998→1.066)、M-04符号反転は4.32%→94.57%へ
      激増した(Issue #10)。訓練指標と閉ループ安全性は別物である。
- [ ] 平衡点(または代表的な動作点)近傍のヤコビアン固有値(スペクトル半径)を算出し、
      安定であることを確認したか(M-02)。
      根拠: eval_stability.py。
- [ ] 入出力方向整合性 ∂(出力の変化)/∂(入力トルク) の符号を、動作軌道全体で確認したか
      (M-04)。符号反転領域があれば記録し、閉ループでの正のフィードバックリスクとして
      注視する。
      根拠: check_sign_consistency.py。単振り子PoCでは4.32%の符号反転が残存(#10)。
- [ ] **内部潜在状態の各成分を物理量(θ̇等)として読み替えて使う場合、その対応関係が
      実際に検証されているか**(教師信号や制約なしに、初期値だけ物理量で与えられている
      という理由だけで、その後も物理量であり続けると仮定していないか)。
      根拠: n_x=2のNSSで、内部状態の2番目の成分をθ̇として閉ループフィードバックに
      流用した(Issue #19)が、この成分はθ(出力)以外に教師信号を持たず、真のθ̇とは
      明確に異なる数値・推移を示すことが判明した(Issue #21)。たまたま今回は有限差分
      によるノイズ増幅を避けるという目的には有効に機能したが、これは「θ̇として正しい」
      ことが検証された結果ではなく、「使ってみたら都合よく動いた」に留まる。物理量として
      正式に採用する場合は、g_φの出力を多次元化して当該量を直接教師するか(仕様書§3.1
      Step2)、運動学的制約をアーキテクチャに組み込む(M-03)こと。
      **Issue #23でStep2(g_φを[θ,θ̇]の2次元化)を実装・検証した結果**、閉ループ発散は
      内部状態流用版(#19)よりさらに広いゲイン範囲(Kd=0.02〜2.00で発散なし)で解消し、
      ゼロ入力エネルギー収束・平衡点安定性も改善したが、**M-04符号反転は4.32%→6.86%へ
      悪化した**。閉ループ安定性とM-04(正のフィードバックリスク)は別の指標であり、
      片方の改善がもう片方を保証しないことに注意(トレードオフとして両方を確認すること)。

## 3. 離散化・サンプリングレート設計

- [ ] **学習時に想定したサロゲート動作周期と、閉ループ実行時に実際に呼び出す周期が
      一致しているか**。
      根拠: 学習時rate(200Hz)と異なる頻度でサロゲートを呼ぶ実験(runtime_ratioを
      40→20,10に変更)で、高頻度側で発散した(Issue #15実験D)。
- [ ] コントローラの微分項(D項・速度フィードバック)は、サロゲートの出力更新周期に対して
      過大なゲインになっていないか。**有限差分による速度推定は、割る時間刻みが短いほど
      ノイズを増幅する**(周期を1/2にすると増幅率は2倍)。
      根拠: サロゲート出力の有限差分θ̇推定(5ms周期→200倍増幅)が、モデルの微小な
      予測誤差をKd項経由で飽和・振動する制御指令に増幅し、発散を引き起こした
      (Issue #12)。**この機構は実験D(離散化不整合)と実験E(ゲイン過大)の両方で
      同一であり、両者は独立した原因ではなく密結合したリスクとして扱うこと**。
      対策: 可能であれば速度フィードバックを有限差分ではなくモデル出力(または内部
      状態)から直接取得すること(Issue #19)。ただし内部状態を流用する場合は本章
      冒頭の「内部潜在状態を物理量として読み替える場合の検証」項目も併せて満たすこと。
- [ ] サロゲートへの入力(トルク)のサンプリング規約(瞬時値サンプル or 区間平均)が、
      学習データ生成時と閉ループ実行時で一致しているか(ZOH規約の一貫性、仕様書§6.4)。

## 4. 制御系設計

- [ ] コントローラゲインは物理モデル用の値をそのまま流用していないか。サロゲート特有の
      ゲイン感度(発散が生じる閾値)をスイープして確認したか。
      根拠: closed_loop_compare.mによるKdスイープ。物理ループは全域(Kd=0〜0.09)で
      安定だったが、サロゲートループはKd=0.03〜0.05間に明確な発散閾値があった(#12)。
- [ ] 飽和処理(トルク制限等)は、物理モデル・サロゲートモデルの両ループで同一に
      適用されているか(仕様書§7)。不公平な比較は結論を誤らせる。

## 5. 検証プロセス全体

- [ ] 少なくとも1組のコントローラゲインで、サロゲート閉ループが物理モデル閉ループと
      整合する(発散しない)PTP応答が得られることを確認したか。
- [ ] 発散が生じた場合、ログ(補正トルクτ_fbとサロゲート出力変化Δŷの符号一致フラグ等)
      から、原因(データ/モデル/学習/離散化/制御)のどれに起因するかを切り分けられる
      体制になっているか。
- [ ] 上記チェックリストの各項目について、**「未実施」ではなく「実施して問題なし」を
      明示的に記録**しているか。チェックリストは形骸化しやすいため、各項目の実施結果
      (数値・図)を残すこと(本PoCでは各Issueコメント・reports/以下の図として記録)。

## 参考:単振り子PoCで得た一般化可能な知見のまとめ

1. 1step予測精度と閉ループ安定性は独立した性質であり、片方の良好さがもう片方を
   保証しない。
2. 訓練・検証損失の改善だけを見てモデル選択すると、長時間安定性を悪化させる方向に
   最適化してしまう危険がある。M-02/M-04/長時間ロールアウトは訓練損失とは独立した
   合否判定として必須。
3. 離散化(サンプリングレート)とコントローラゲイン設計は、少なくとも本アーキテクチャ
   (有限差分による速度推定を用いる場合)では独立した問題ではない。
4. 外挿判定(データカバレッジ不足の検出)は必要だが、モデルの不具合を全て説明できる
   わけではない。学習範囲内のフィッティング誤差は別途、固有値・符号整合性等で検出する
   必要がある。
